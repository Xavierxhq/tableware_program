These 3 models are trained using the data with augmentation.

Take chawdoe-hardsample-(o->n5)-68-95.55%.tar as an example:
(o->n5)-n is the model that the o-train-o-test model fine-tuned with n5 and test on n.
and the -68-95.55% means that the model get the top acc. at the end of 68th epoch.
